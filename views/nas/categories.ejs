<!-- RETIRED — local NAS storage has been removed from this application.
     There is no NAS route mounted in app.js and no NAS data model in
     prisma/schema.prisma, so this view is never rendered. It is kept only
     as an inert placeholder because this deployment cannot delete files
     from the repository. -->
